# images

Regenerate the SVGs with `python docs/make_images.py`.

`founder.jpg` is the one file that is not generated; place it by hand. It is the
author's photograph, referenced from "The engineer behind it" in
`docs/product-en.md`. Aim for at least 400px on the short side and something
close to square.
